-- Implement a PL/SQL block with a cursor to fetch and display the Name and Salary of the
-- top 5 highest-paid employees from the Staff table. - 

CREATE TABLE Staff (
    Staff_ID int PRIMARY KEY,
    Name VARCHAR(50),
    Department VARCHAR(30),
    Salary int
);

INSERT INTO Staff VALUES
(101, 'Rahul Sharma', 'IT', 55000),
(102, 'Priya Singh', 'HR', 48000),
(103, 'Amit Kumar', 'Finance', 72000),
(104, 'Neha Gupta', 'IT', 85000),
(105, 'Rohit Verma', 'Sales', 62000),
(106, 'Anjali Mehta', 'Finance', 95000),
(107, 'Karan Patel', 'IT', 78000),
(108, 'Sneha Rao', 'HR', 51000),
(109, 'Vikash Yadav', 'Sales', 68000),
(110, 'Pooja Sharma', 'Finance', 90000);

select * from Staff;




DO $$
DECLARE
    emp_cursor CURSOR FOR
        SELECT Name, Salary
        FROM Staff
        ORDER BY Salary DESC
        LIMIT 5;

    emp_record RECORD;

BEGIN
    OPEN emp_cursor;

    LOOP
        FETCH emp_cursor INTO emp_record;

        EXIT WHEN NOT FOUND;

        RAISE NOTICE 'Name: % | Salary: %',
            emp_record.Name,
            emp_record.Salary;
    END LOOP;

    CLOSE emp_cursor;
END $$;
